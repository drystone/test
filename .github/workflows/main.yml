---
name: main-push-n-pull

# yamllint complains about "on" so ...
# yamllint disable-line
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  foo-bar:
    runs-on: ubuntu-latest
    steps:
      - name: foo-bar
        run: ./ci/foo-bar
